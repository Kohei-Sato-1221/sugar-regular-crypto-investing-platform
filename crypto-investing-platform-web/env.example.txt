# Database
# PostgreSQLデータベースの接続URL
DATABASE_URL="postgresql://user:password@localhost:5432/database_name?schema=public"

# AWS Cognito Configuration
# 開発環境ではオプショナル、本番環境では必須
# Cognito User Pool ID
COGNITO_USER_POOL_ID="ap-northeast-1_xxxxxxxxx"

# Cognito App Client ID
COGNITO_CLIENT_ID="xxxxxxxxxxxxxxxxxxxxxxxxxx"

# Cognito App Client Secret (Confidential Clientの場合のみ必要)
# 取得方法: terraform/environments/production で `terraform output -raw cognito_user_pool_client_secret`
COGNITO_CLIENT_SECRET=""

# AWS Region (デフォルト: ap-northeast-1)
AWS_REGION="ap-northeast-1"

# AWS Credentials (オプショナル)
# AWS CLIの認証情報を使用する場合は設定不要
# 明示的に設定する場合のみ以下を設定
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""

